# E-Commerce_Sales_Data_Analysis_SQL
This project demonstrates SQL data analysis on an E-Commerce database. It includes data extraction using SELECT, filtering with WHERE, sorting using ORDER BY, grouping with GROUP BY, JOIN operations, subqueries, aggregate functions, views, and query optimization using indexes to generate meaningful business insights.
